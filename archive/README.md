# Archive — 阶段性成果归档

本目录保存迁移至 `vue-element-plus-admin` 底座之前的前端成果，便于对照与回滚。

| 目录 | 说明 |
|------|------|
| [`pwms-web-v3/`](./pwms-web-v3/) | 原 `web/` 业务前端（含深色主题改造、业务闭环） |
| [`pwms-design-theme/`](./pwms-design-theme/) | 原 `design/ui-theme` 样式规范沉淀 |

当前主工程：仓库根目录 [`web/`](../web/)，基于 `reference/vue-element-plus-admin-master`，业务代码位于 `web/src/views/Pwms`、`web/src/stores` 等。
